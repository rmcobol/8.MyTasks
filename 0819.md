이제 데이터 저장/불러오기 로직을 구현해줘.

- 할 일 데이터 구조: { id, title, category, completed, createdAt }
- localStorage 키는 'todos'로 사용하고 값은 할 일 객체 배열을 JSON으로 저장
- 다음 함수들을 만들어줘:
    1. loadTodos() — localStorage에서 데이터를 불러와 배열로 반환 (데이터 없으면 빈 배열)
    2. saveTodos(todos) — 배열을 받아 localStorage에 JSON으로 저장
    3. addTodo(title, category) — 새 할 일 객체를 생성해 배열에 추가하고 저장
    4. updateTodo(id, changes) — id로 찾아 해당 필드만 수정 후 저장
    5. deleteTodo(id) — id로 찾아 배열에서 제거 후 저장
    6. toggleComplete(id) — completed 값을 반전시키고 저장
- id는 타임스탬프나 간단한 UUID 방식으로 생성해줘
- 이 단계에서는 화면 렌더링은 하지 말고, 콘솔에 로그를 찍어서 각 함수가 정상 동작하는지 테스트해줘
