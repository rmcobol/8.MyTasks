필터링과 진행률 표시 기능을 추가해줘.

- 필터 버튼(전체/업무/개인/공부) 클릭 시 해당 카테고리 항목만 목록에 표시
    - 현재 선택된 필터 상태를 변수로 관리하고, 선택된 버튼은 시각적으로 활성화 표시
    - render() 함수가 현재 필터 상태를 반영해서 목록을 그리도록 수정
- 진행률 계산 및 표시
    - 전체 할 일 개수 대비 완료된 할 일 개수를 % 로 계산
    - 상단 진행률 영역에 프로그레스 바(또는 원형 그래프)와 "5/12 완료 · 42%" 형태의 텍스트로 표시
    - todos가 변경될 때마다(추가/삭제/완료 토글) 진행률도 함께 갱신
- 카테고리별 색상 태그를 CSS로 구분해줘 (예: 업무-파랑, 개인-초록, 공부-주황)
